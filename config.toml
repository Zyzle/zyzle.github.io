# The URL the site will be built for
base_url = "https://zyzle.dev"

title = "Zyzle.dev"
default_language = "en"
minify_html = true

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

taxonomies = [
    { name = "tags", feed = false },
    { name = "language", feed = false }
]

generate_feed = true
feed_filename = "atom.xml"
external_links_target_blank = true

[markdown]
# Whether to do syntax highlightingß
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
extra_syntaxes_and_themes = ["extra_highlight_themes"]
highlight_theme = "OD"

[extra]
comments_repo = "zyzle.github.io"
comments_repo_owner = "Zyzle"
comments_oauth_client_id = "b60275ae52523320945b"
comments_lynx_app = "desolate-reaches-82509.herokuapp.com"
